Day 3 Objective (Pre-Commitment)

Tomorrow’s 45-minute target:

Implement deterministic evaluation logic.

Specifically:

- Fake LLM client returns hardcoded string.

Evaluator counts:

- sentences
- words
- characters

Apply scoring formula exactly as defined.

Return structured JSON matching spec example.


Set up GitHub Actions for CI/CD:

Linting

Testing

Build

Push to Docker Hub

Deploy to Render